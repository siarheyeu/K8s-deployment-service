# K8s-deployment-service
A minimal Kubernetes demo showing how to deploy an application using a Deployment and expose it using a Service. Includes YAML manifests for pods, replicas, labels, selectors, and ClusterIP/NodePort service types.
